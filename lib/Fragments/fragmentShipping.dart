import 'package:flutter/material.dart';

class FragmentShipping extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("fragmentShipping"),
    );
  }
}